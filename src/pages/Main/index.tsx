import { AppScreen } from "@stackflow/plugin-basic-ui";
import { Header } from "@/components";

const Main = () => {
  return (
    <AppScreen>
      <Header route="main" />
    </AppScreen>
  );
};

export default Main;
